# 이 파일을 .env 로 복사해서 실제 값을 채운다. .env 는 커밋되지 않는다.

# BotFather 에서 발급받은 봇 토큰
# 노출되면 즉시 BotFather → /revoke 로 재발급할 것
TELEGRAM_BOT_TOKEN=

# 채널 식별자 (@채널이름 또는 -100으로 시작하는 숫자 ID)
TELEGRAM_CHANNEL_ID=@your_channel

# 채널에 연결된 댓글 그룹 ID (3~4주차부터 사용)
TELEGRAM_DISCUSSION_GROUP_ID=

# 발행 시간 기준 타임존
TZ=Asia/Seoul
